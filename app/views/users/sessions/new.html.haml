.devise
  .devise__logo
    Log in
  = form_for(resource, as: resource_name, url: session_path(resource_name)) do |f|
    .devise__field
      .devise__field__label
        = f.label :email,"Email"
      = f.email_field :email, autofocus: true, autocomplete: "email",class: "devise__field__form"
    .devise__field
      .devise__field__label
        = f.label :password,"パスワード"
      = f.password_field :password, autocomplete: "current-password",class: "devise__field__form"
    - if devise_mapping.rememberable?
      .devise__check
        = f.check_box :remember_me
        = f.label :remember_me,"パスワードを保存しますか？"
    .devise__actions
      = f.submit "● Log in ●",class: "devise__actions__btn"
  = render "users/shared/links"
